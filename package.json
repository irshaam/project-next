{
  "name": "project-next",
  "description": "A modular news platform powered by interactive and visual data-driven content that elevates journalism to a new level.",
  "version": "0.0.1",
  "private": "true",
  "author": "Irshaam <mohamed.irshaam@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/CIAOadvertising/project-next.git"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "build": "lerna run build --parallel",
    "clean": "lerna run clean && lerna clean -y",
    "dev": "lerna run dev --parallel",
    "lint": "lerna run lint --parallel"
  },
  "license": "UNLICENSED",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-lerna-scopes": "^12.1.4",
    "eslint": "^7.32.0",
    "husky": "^6.0.0",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "typescript": "^4.3.5"
  }
}
